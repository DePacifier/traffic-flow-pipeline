with open('./I80_davis.txt', mode='r') as readfile:
    for index, line in enumerate(readfile):
        # print(index, ". ", line)
        # if(index > 50):
        #     break
        pass
    print(index + 1)
